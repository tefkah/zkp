// import { NoteData } from '@zkp/redis'
// import { ChangeEventHandler, useState } from 'react'

export const CarForm = () => {
  // const [hits, setHits] = useState<NoteData[]>([])

  // const search: ChangeEventHandler<HTMLInputElement> = async (event) => {
  //   const q = event?.target?.value
  console.log('a')
  //   if (q.length > 2) {
  //     const params = new URLSearchParams({ q })

  //     const res = await fetch(`/api/search?${params}`)

  //     const result = await res.json()
  //     console.log(result)
  //     setHits(result.cars)
  //   }
  // }

  return (
    <div>
      {/* <input onChange={search} type="text" placeholder="search notes..." className="form-control" />

      <ul className="flex flex-col items-start ">
        {hits.map((hit) => (
          <li className="flex justify-between " key={hit.entityId}>
             <img width="50px" src={hit.image} />

            <div className="">
              <div className="text-bold">{hit.title}</div>
              {hit.body}
            </div>
          </li>
        ))}
      </ul >
    */}
    </div>
  )
}
